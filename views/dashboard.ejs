<!DOCTYPE html>
<html lang="en">
<head>
  <title>Dashboard | MyDrive</title>
  <style>
    body {
      margin: 0;
      font-family: 'Segoe UI', sans-serif;
      background-color: #111827;
      color: #f3f4f6;
    }
    main {
      padding: 2rem 3rem;
    }
    h2 {
      font-size: 28px;
      color: #f9fafb;
      margin-bottom: 1rem;
    }
    h3 {
      font-size: 20px;
      margin: 2.5rem 0 1rem;
      color: #d1d5db;
    }
    .form-group {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      margin-bottom: 1.5rem;
    }
    input[type="text"], input[type="file"] {
      padding: 10px;
      border-radius: 6px;
      border: 1px solid #4b5563;
      font-size: 14px;
      background-color: #1f2937;
      color: #f9fafb;
      flex: 1;
    }
    button {
      padding: 10px 16px;
      border-radius: 6px;
      font-size: 14px;
      font-weight: bold;
      border: none;
      cursor: pointer;
    }
    .btn-green { background-color: #10b981; color: white; }
    .btn-green:hover { background-color: #059669; }
    .btn-blue { background-color: #3b82f6; color: white; }
    .btn-blue:hover { background-color: #2563eb; }
    .btn-yellow { background-color: #facc15; color: #1f2937; }
    .btn-yellow:hover { background-color: #eab308; }
    .btn-red { background-color: #ef4444; color: white; }
    .btn-red:hover { background-color: #dc2626; }

    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));
      gap: 1.2rem;
    }
    .card {
      background: #1f2937;
      padding: 1rem;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0,0,0,0.3);
    }
    .card-title {
      font-weight: 600;
      font-size: 16px;
      margin-bottom: 0.5rem;
      color: #f9fafb;
      display: flex;
      align-items: center;
      gap: 5px;
    }
    .card form {
      display: flex;
      flex-direction: column;
      gap: 8px;
      margin-top: 0.5rem;
    }
    .card form button {
      width: fit-content;
      align-self: flex-start;
    }
    .card a {
      color: #60a5fa;
      font-size: 13px;
      text-decoration: none;
    }
    .card a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <%- include('partials/header') %>
  <main>
    <h2>Your Files & Folders</h2>

    <!-- Create Folder -->
    <form action="/folder" method="POST" class="form-group">
      <input type="text" name="name" placeholder="New Folder Name" required />
      <button class="btn-green" type="submit">Create Folder</button>
    </form>

    <!-- Upload File -->
    <form action="/upload" method="POST" enctype="multipart/form-data" class="form-group">
      <input type="file" name="file" required />
      <button class="btn-blue" type="submit">Upload File</button>
    </form>

    <!-- Folders -->
    <h3>📁 Folders</h3>
    <div class="grid">
      <% folders.forEach(folder => { %>
        <div class="card">
          <div class="card-title">📁 <%= folder.name %></div>
          <form action="/rename" method="POST">
            <input type="hidden" name="id" value="<%= folder._id %>">
            <input type="hidden" name="type" value="folder">
            <input type="text" name="name" placeholder="Rename Folder" />
            <button class="btn-yellow" type="submit">Rename</button>
          </form>
          <form action="/delete" method="POST">
            <input type="hidden" name="id" value="<%= folder._id %>">
            <input type="hidden" name="type" value="folder">
            <button class="btn-red" type="submit">Delete</button>
          </form>
        </div>
      <% }) %>
    </div>

    <!-- Files -->
    <h3>📄 Files</h3>
    <div class="grid">
      <% files.forEach(file => { %>
        <div class="card">
          <div class="card-title">📄 <%= file.filename %></div>
          <a href="/download/<%= file._id %>">Download</a>
          <form action="/rename" method="POST">
            <input type="hidden" name="id" value="<%= file._id %>">
            <input type="hidden" name="type" value="file">
            <input type="text" name="name" placeholder="Rename File" />
            <button class="btn-yellow" type="submit">Rename</button>
          </form>
          <form action="/delete" method="POST">
            <input type="hidden" name="id" value="<%= file._id %>">
            <input type="hidden" name="type" value="file">
            <button class="btn-red" type="submit">Delete</button>
          </form>
        </div>
      <% }) %>
    </div>
  </main>
</body>
</html>
